/* global describe, it, beforeEach, expect */

describe('someTest', () => {
  beforeEach(() => {
  });

  describe('Will it blend?', () => {
    it('Should blend', () => {
      expect(true).toEqual(true);
    });
  });
});
